# Security policy

Please report a suspected path escape, cross-Session frame leak, request-policy bypass or unintended network/file capability privately through the repository's GitHub security advisory page.

Do not include credentials, private browser data or user project files in a public issue.

Supported security fixes currently target the latest `0.1.x` prerelease only.
